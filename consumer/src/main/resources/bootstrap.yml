#使用nacao配置管理
spring:
  application:
    name: consumer
  cloud:
    nacos:
      config:
        server-addr: 192.168.31.125:8848
        file-extension: yml #配置文件类型。

  # 多环境选择
  profiles:
    active:
